# 学习进度追踪方案 (Tauri 桌面应用版)

## 📋 方案概述

一个用于追踪个人学习进度的轻量级桌面应用,基于 **Tauri + Vue3** 构建。自动扫描本地学习目录 `/Users/imber/Desktop/imber`,解析 Markdown 笔记,生成可视化学习进度面板。

**为什么选择 Tauri 桌面应用?**
- ✅ 直接访问本地文件系统,无需手动导出数据
- ✅ 实时监听文件变化,自动更新统计
- ✅ 点击笔记链接直接打开本地文件
- ✅ 包体积小(~10MB),启动快,内存占用低
- ✅ 可常驻系统托盘,随时查看学习进度
- ✅ 支持桌面通知和全局快捷键

---

## 🎯 核心功能

### 1. 学习记录管理
- **每日学习日志**: 记录每天学习的内容、时长、笔记链接
- **自动扫描笔记**: 扫描 `/Users/imber/Desktop/imber` 目录,自动识别 Markdown 文件
- **实时监听**: 文件变化自动更新统计数据
- **快速记录**: 全局快捷键快速打开今日学习记录窗口

### 2. 可视化展示
- **学习日历热力图**: 类似 GitHub Contributions,显示学习频率
- **进度仪表盘**: 各学习路径的完成百分比
- **知识树状图**: 层级展示知识体系和掌握情况
- **时间线视图**: 按时间轴展示学习历程
- **笔记预览**: 点击笔记链接在应用内预览 Markdown

### 4. 统计分析
- **系统托盘**: 常驻托盘,显示今日学习时长
- **桌面通知**: 学习提醒、目标达成通知
- **快捷键**: `Cmd+Shift+L` 快速打开记录窗口
- **本地文件操作**: 直接打开、编辑学习笔记
- **数据导出**: 导出学习报告为 PDF/Markdown
- 累计学习时长
- 每周/每月学习趋势
- 各领域投入时间占比
- 知识点掌握数量统计

---

## 📊 项目结构设计

### Tauri 应用目录结构
```
visual-learn/
│
├── src-tauri/                      # Tauri 后端 (Rust)
│   ├── src/
│   │   ├── main.rs                 # 主进程入口
│   │   ├── commands/               # Tauri Commands (前端调用的 API)
│   │   │   ├── mod.rs
│   │   │   ├── file_scanner.rs    # 文件扫描模块
│   │   │   ├── daily_log.rs       # 每日记录管理
│   │   │   ├── roadmap.rs         # 学习路线管理
│   │   │   ├── statistics.rs      # 统计分析
│   │   │   └── notes.rs           # 笔记操作
│   │   ├── models/                 # 数据模型
│   │   │   ├── mod.rs
│   │   │   ├── daily_log.rs
│   │   │   ├── roadmap.rs
│   │   │   └── knowledge.rs
│   │   ├── utils/                  # 工具函数
│   │   │   ├── mod.rs
│   │   │   ├── markdown_parser.rs # Markdown 解析
│   │   │   ├── file_watcher.rs    # 文件监听
│   │   │   └── date_utils.rs
│   │   └── config.rs               # 配置管理
│   ├── Cargo.toml                  # Rust 依赖
│   ├── tauri.conf.json             # Tauri 配置
│   └── icons/                      # 应用图标
│
├── src/                            # 前端 (React + TypeScript)
│   ├── main.tsx                    # 前端入口
│   ├── App.tsx                     # 根组件
│   ├── router/                     # 路由配置
│   │   └── index.tsx
│   ├── stores/                     # Zustand 状态管理
│   │   ├── dailyStore.ts          # 每日记录状态
│   │   ├── roadmapStore.ts        # 路线图状态
│   │   ├── statisticsStore.ts     # 统计数据状态
│   │   └── settingsStore.ts       # 设置状态
│   ├── pages/                      # 页面组件
│   │   ├── Dashboard.tsx          # 主仪表盘
│   │   ├── DailyLog.tsx           # 每日记录页
│   │   ├── RoadmapView.tsx        # 学习路线页
│   │   ├── KnowledgeTree.tsx      # 知识树页
│   │   ├── NotesLibrary.tsx       # 笔记库页
│   │   └── Settings.tsx           # 设置页
│   ├── components/                 # 可复用组件
│   │   ├── charts/
│   │   │   ├── HeatmapCalendar.tsx    # 热力图
│   │   │   ├── ProgressBar.tsx        # 进度条
│   │   │   ├── TrendChart.tsx         # 趋势图
│   │   │   └── DonutChart.tsx         # 环形图
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Header.tsx
│   │   │   └── TrayMenu.tsx
│   │   ├── NoteCard.tsx           # 笔记卡片
│   │   ├── MarkdownPreview.tsx    # Markdown 预览
│   │   └── QuickLogModal.tsx      # 快速记录弹窗
│   ├── hooks/                      # 自定义 Hooks
│   │   ├── useTauriCommand.ts     # Tauri 命令封装
│   │   ├── useFileWatcher.ts      # 文件监听
│   │   └── useNotification.ts     # 通知封装
│   ├── types/                      # TypeScript 类型定义
│   │   ├── daily-log.ts
│   │   ├── roadmap.ts
│   │   └── statistics.ts
│   ├── assets/                     # 静态资源
│   │   ├── styles/
│   │   └── images/
│   └── utils/                      # 工具函数
│       ├── date.ts
│       └── format.ts
│
├── data/                           # 数据目录 (同之前)
│   ├── daily/                      # 每日学习记录
│   │   └── 2026/
│   │       └── 06/
│   ├── roadmaps/                   # 学习路线图
│   └── knowledge-base/             # 知识库索引
│
├── package.json                    # 前端依赖
├── tsconfig.json                   # TypeScript 配置
├── vite.config.ts                  # Vite 配置
├── tailwind.config.js              # Tailwind CSS 配置 (可选)
└── README.md                       # 使用说明
```

---

## 📝 数据格式规范

### 每日学习记录 (`data/daily/2026/06/2026-06-12.md`)

```markdown
---
date: 2026-06-12
dayOfWeek: 星期四
totalMinutes: 180
tags: [python, agent, database]
mood: 😊
---

# 2026年6月12日 学习记录

## ✅ 今日完成

### 🐍 Python后端 (90分钟)
- **学习内容**: NumPy数组操作与切片
- **笔记链接**: [NumPy基础](/Users/imber/Desktop/imber/后端/python/python小册/20-NumPy基础.md)
- **练习代码**: [numpy-demo.py](/Users/imber/Desktop/imber/后端/python/python-demo/numpy-demo.py)
- **掌握程度**: ⭐⭐⭐⭐☆ (4/5)
- **关键收获**:
  - 理解了数组切片的底层机制
  - 掌握了广播(broadcasting)概念
  - 完成了矩阵运算练习

### 🤖 Agent开发 (60分钟)
- **学习内容**: 大模型应用开发全景图
- **笔记链接**: [03-大模型应用开发全景图](/Users/imber/Desktop/imber/Agent/agent小册/03-大模型应用开发全景图.md)
- **掌握程度**: ⭐⭐⭐☆☆ (3/5)
- **关键收获**:
  - 了解了RAG的基本架构
  - 理解了Agent的工作流程

### 🗄️ 数据库 (30分钟)
- **学习内容**: 复习SQL查询优化
- **练习题**: 10道索引优化题
- **掌握程度**: ⭐⭐⭐⭐☆ (4/5)

## 📌 今日遇到的问题

1. **问题**: NumPy广播机制在多维数组中的规则不太清楚
   - **解决方案**: 查阅官方文档,画图理解维度对齐
   - **状态**: ✅ 已解决

2. **问题**: Agent中的Memory机制实现细节不明确
   - **解决方案**: 待查阅相关源码
   - **状态**: ⏳ 待解决

## 🎯 明日计划

- [ ] 学习Pandas数据处理 (预计90分钟)
- [ ] 完成Agent工具调用实战 (预计60分钟)
- [ ] 复习Python装饰器 (预计30分钟)

## 💭 今日反思

今天学习效率不错,NumPy的学习比预期顺利。但Agent部分概念较多,需要多看几遍加深理解。明天重点攻克Pandas。

---

**学习时长**: 3小时  
**完成任务**: 3/3  
**学习状态**: 良好 😊
```

---

### 学习路线图 (`data/roadmaps/backend-python.md`)

```markdown
---
roadmap: Python后端开发
category: 后端
startDate: 2026-06-01
targetDate: 2026-09-01
progress: 35
status: in-progress
---

# Python后端开发学习路线

## 📊 整体进度: 35%

### 阶段一: Python基础 ✅ (100%)

- [x] Python环境配置
- [x] 第一个Python程序
- [x] Python与JavaScript对比
- [x] 数据类型与变量
- [x] 控制流程

### 阶段二: Python进阶 🔄 (60%)

- [x] 函数与模块
- [x] 面向对象编程
- [x] 异常处理
- [x] 文件操作
- [ ] 装饰器与元编程 (学习中)
- [ ] 并发编程
- [ ] 异步IO

### 阶段三: Web框架 📅 (0%)

- [ ] Flask入门
- [ ] Django基础
- [ ] RESTful API设计
- [ ] 数据库ORM
- [ ] 身份认证与授权

### 阶段四: 数据处理 🔄 (40%)

- [x] NumPy基础 (刚完成)
- [ ] Pandas数据处理 (下一个)
- [ ] 数据可视化
- [ ] 数据库操作进阶

### 阶段五: AI应用开发 📅 (0%)

- [ ] 调用大模型API
- [ ] 向量与Embedding
- [ ] RAG应用开发
- [ ] Agent开发实战

---

## 📈 学习数据

- **开始日期**: 2026-06-01
- **累计学习天数**: 12天
- **累计学习时长**: 28小时
- **完成知识点**: 24/68
- **预计完成日期**: 2026-09-01
```

---

### 知识库索引 (`data/knowledge-base/backend.json`)

```json
{
  "category": "后端开发",
  "lastUpdate": "2026-06-12",
  "knowledgeTree": [
    {
      "id": "python",
      "name": "Python",
      "level": 1,
      "status": "learning",
      "progress": 45,
      "children": [
        {
          "id": "python-basic",
          "name": "Python基础",
          "level": 2,
          "status": "completed",
          "progress": 100,
          "studyTime": 600,
          "noteLinks": [
            "/Users/imber/Desktop/imber/后端/python/python小册/01-Python环境配置.md",
            "/Users/imber/Desktop/imber/后端/python/python小册/02-第一个Python程序.md"
          ],
          "children": [
            {
              "id": "python-env",
              "name": "环境配置",
              "level": 3,
              "status": "completed",
              "masteryLevel": 5,
              "lastReview": "2026-06-05",
              "tags": ["环境", "工具"]
            },
            {
              "id": "python-syntax",
              "name": "语法基础",
              "level": 3,
              "status": "completed",
              "masteryLevel": 5,
              "lastReview": "2026-06-06",
              "tags": ["语法"]
            }
          ]
        },
        {
          "id": "python-advanced",
          "name": "Python进阶",
          "level": 2,
          "status": "learning",
          "progress": 60,
          "studyTime": 720,
          "children": [
            {
              "id": "python-decorator",
              "name": "装饰器",
              "level": 3,
              "status": "learning",
              "masteryLevel": 3,
              "lastStudy": "2026-06-10",
              "tags": ["进阶", "函数式编程"]
            }
          ]
        },
        {
          "id": "python-data",
          "name": "数据处理",
          "level": 2,
          "status": "learning",
          "progress": 40,
          "studyTime": 360,
          "children": [
            {
              "id": "numpy",
              "name": "NumPy",
              "level": 3,
              "status": "completed",
              "masteryLevel": 4,
              "lastStudy": "2026-06-12",
              "tags": ["数据处理", "数组"]
            },
            {
              "id": "pandas",
              "name": "Pandas",
              "level": 3,
              "status": "todo",
              "masteryLevel": 0,
              "tags": ["数据处理", "DataFrame"]
            }
          ]
        }
      ]
    },
    {
      "id": "database",
      "name": "数据库",
      "level": 1,
      "status": "learning",
      "progress": 30,
      "children": []
    }
  ],
  "statistics": {
    "totalKnowledgePoints": 68,
    "completedPoints": 24,
    "learningPoints": 15,
    "todoPoints": 29,
    "totalStudyTime": 1680
  }
}
```

---

## 🖥️ 可视化界面设计

### 主界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  🎓 Imber 学习进度追踪系统                    [今日] [本周]  │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  📊 学习概览                                                  │
│  ┌──────────┬──────────┬──────────┬──────────┐             │
│  │ 总学习时长 │ 连续学习  │ 知识点数  │ 本周时长  │             │
│  │  128小时  │  12天    │  24/68  │  21小时   │             │
│  └──────────┴──────────┴──────────┴──────────┘             │
│                                                               │
│  📅 学习日历热力图                                            │
│  ┌─────────────────────────────────────────────┐            │
│  │ 6月  ░░▓▓▓░░▓▓░░▓▓▓░░▓░░                   │            │
│  │ 5月  ▓▓░░▓▓▓░░▓░░░░▓▓▓░░                   │            │
│  └─────────────────────────────────────────────┘            │
│       ░ 0   ▒ 1-2h   ▓ 2-4h   █ 4h+                         │
│                                                               │
│  🎯 学习路径进度                                              │
│  ┌─────────────────────────────────────────────┐            │
│  │ Python后端   ████████░░░░░░░░░░░░ 35%      │            │
│  │ AI编程      ████░░░░░░░░░░░░░░░░░ 20%      │            │
│  │ Agent开发   ██████░░░░░░░░░░░░░░░ 25%      │            │
│  │ 数据库      ██████████░░░░░░░░░░░ 45%      │            │
│  └─────────────────────────────────────────────┘            │
│                                                               │
│  🌳 知识树状图                [展开] [折叠]                    │
│  ┌─────────────────────────────────────────────┐            │
│  │ ▼ 后端开发 (45%)                            │            │
│  │   ▼ Python (45%)                            │            │
│  │     ✅ Python基础 (100%)                   │            │
│  │     🔄 Python进阶 (60%)                    │            │
│  │       ✅ 函数与模块                         │            │
│  │       🔄 装饰器 (学习中)                    │            │
│  │       📅 并发编程 (待学习)                  │            │
│  │     🔄 数据处理 (40%)                       │            │
│  │       ✅ NumPy ⭐⭐⭐⭐☆                    │            │
│  │       📅 Pandas (下一个)                   │            │
│  │   ▼ 数据库 (30%)                            │            │
│  └─────────────────────────────────────────────┘            │
│                                                               │
│  📈 学习趋势 (最近7天)                                        │
│  ┌─────────────────────────────────────────────┐            │
│  │  4h ┤     ╭─╮                               │            │
│  │  3h ┤   ╭─╯ ╰─╮                             │            │
│  │  2h ┤ ╭─╯     ╰─╮   ╭─╮                     │            │
│  │  1h ┤─╯         ╰───╯ ╰─                    │            │
│  │  0h └───────────────────────                │            │
│  │      一 二 三 四 五 六 日                    │            │
│  └─────────────────────────────────────────────┘            │
│                                                               │
│  🔥 本周学习亮点                                              │
│  • 完成NumPy基础学习,掌握数组操作                            │
│  • 连续学习12天,保持良好习惯                                 │
│  • Python进阶进度达到60%                                     │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 技术栈

### 桌面应用框架
- **Tauri 2.0**: 轻量级桌面应用框架
  - 包体积小 (~10MB vs Electron ~120MB)
  - 内存占用低 (使用系统 WebView)
  - Rust 后端性能优秀

### 前端技术
- **框架**: React 18 + TypeScript
- **构建工具**: Vite
- **状态管理**: Zustand (轻量级) / Redux Toolkit
- **路由**: React Router
- **UI 组件**: 
  - Ant Design (组件库)
  - Tailwind CSS (样式)
- **图表库**: 
  - ECharts + echarts-for-react
  - cal-heatmap (日历热力图)
- **Markdown**: 
  - react-markdown (渲染)
  - remark-gfm (GitHub 风格)
  - rehype-highlight (代码高亮)

### 后端 (Rust)
- **Tauri API**: 文件系统、窗口管理、系统托盘
- **依赖库**:
  - `serde`: JSON 序列化
  - `tokio`: 异步运行时
  - `notify`: 文件监听
  - `pulldown-cmark`: Markdown 解析
  - `chrono`: 日期时间处理
  - `yaml-rust`: YAML 配置解析

### 数据存储
- **主要方式**: Markdown 文件 + JSON 索引
- **缓存**: SQLite (可选,用于加速查询)
- **配置**: YAML 文件

---

## 🔧 核心功能实现

### 1. 文件系统访问 (Tauri Commands)

**Rust 后端实现**:

```rust
// src-tauri/src/commands/file_scanner.rs

use std::path::Path;
use serde::{Serialize, Deserialize};

/// 笔记文件元数据
#[derive(Debug, Serialize, Deserialize)]
pub struct NoteFile {
    pub path: String,           // 文件路径
    pub name: String,           // 文件名
    pub category: String,       // 分类 (后端/AI/Agent)
    pub size: u64,              // 文件大小
    pub modified: i64,          // 修改时间戳
    pub word_count: usize,      // 字数
}

/// 扫描学习目录
#[tauri::command]
pub async fn scan_study_notes(study_root: String) -> Result<Vec<NoteFile>, String> {
    let path = Path::new(&study_root);
    let mut notes = Vec::new();
    
    // 递归扫描 .md 文件
    scan_directory(path, &mut notes)?;
    
    Ok(notes)
}

/// 读取每日学习记录
#[tauri::command]
pub async fn read_daily_log(date: String) -> Result<DailyLog, String> {
    let file_path = format!("./data/daily/{}.md", date);
    let content = std::fs::read_to_string(&file_path)
        .map_err(|e| format!("读取文件失败: {}", e))?;
    
    // 解析 Markdown frontmatter 和内容
    let log = parse_daily_log(&content)?;
    
    Ok(log)
}

/// 保存每日学习记录
#[tauri::command]
pub async fn save_daily_log(log: DailyLog) -> Result<(), String> {
    let file_path = format!("./data/daily/{}.md", log.date);
    let content = serialize_daily_log(&log)?;
    
    std::fs::write(&file_path, content)
        .map_err(|e| format!("保存文件失败: {}", e))?;
    
    Ok(())
}

/// 用系统默认编辑器打开笔记
#[tauri::command]
pub async fn open_note_in_editor(file_path: String) -> Result<(), String> {
    #[cfg(target_os = "macos")]
    {
        std::process::Command::new("open")
            .arg(&file_path)
            .spawn()
            .map_err(|e| format!("打开文件失败: {}", e))?;
    }
    
    Ok(())
}

/// 获取学习统计数据
#[tauri::command]
pub async fn get_statistics() -> Result<Statistics, String> {
    // 读取所有每日记录,计算统计数据
    let stats = calculate_statistics().await?;
    Ok(stats)
}
```

---

### 2. 文件监听 (实时更新)

```rust
// src-tauri/src/utils/file_watcher.rs

use notify::{Watcher, RecursiveMode, Event};
use tauri::{AppHandle, Manager};

/// 启动文件监听
pub fn start_file_watcher(app_handle: AppHandle) -> Result<(), Box<dyn std::error::Error>> {
    let (tx, rx) = std::sync::mpsc::channel();
    
    let mut watcher = notify::watcher(tx, std::time::Duration::from_secs(2))?;
    
    // 监听学习目录
    watcher.watch("/Users/imber/Desktop/imber", RecursiveMode::Recursive)?;
    
    // 监听数据目录
    watcher.watch("./data", RecursiveMode::Recursive)?;
    
    // 在后台线程处理文件变化事件
    std::thread::spawn(move || {
        loop {
            match rx.recv() {
                Ok(event) => {
                    // 发送事件到前端
                    app_handle.emit_all("files-changed", event).ok();
                }
                Err(e) => eprintln!("文件监听错误: {:?}", e),
            }
        }
    });
    
    Ok(())
}
```

---

### 3. 前端调用 (React Hooks)

```typescript
// src/hooks/useTauriCommand.ts

import { invoke } from '@tauri-apps/api/tauri'
import { listen } from '@tauri-apps/api/event'
import { useState, useCallback } from 'react'
import type { DailyLog, Statistics } from '@/types'

/**
 * 每日学习记录 Hook
 */
export function useDailyLog() {
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState<string | null>(null)
  
  // 读取每日记录
  const readLog = useCallback(async (date: string) => {
    setLoading(true)
    setError(null)
    
    try {
      const log = await invoke<DailyLog>('read_daily_log', { date })
      return log
    } catch (e) {
      const errorMsg = e as string
      setError(errorMsg)
      throw e
    } finally {
      setLoading(false)
    }
  }, [])
  
  // 保存每日记录
  const saveLog = useCallback(async (log: DailyLog) => {
    setLoading(true)
    setError(null)
    
    try {
      await invoke('save_daily_log', { log })
    } catch (e) {
      const errorMsg = e as string
      setError(errorMsg)
      throw e
    } finally {
      setLoading(false)
    }
  }, [])
  
  return {
    loading,
    error,
    readLog,
    saveLog
  }
}

/**
 * 文件监听 Hook
 */
export function useFileWatcher(callback: (payload: any) => void) {
  useEffect(() => {
    const unlisten = listen('files-changed', (event) => {
      console.log('文件已更新:', event.payload)
      callback(event.payload)
    })
    
    return () => {
      unlisten.then(fn => fn())
    }
  }, [callback])
}

/**
 * 笔记操作 Hook
 */
export function useNoteOperations() {
  const openNote = useCallback(async (filePath: string) => {
    try {
      await invoke('open_note_in_editor', { filePath })
    } catch (e) {
      console.error('打开笔记失败:', e)
    }
  }, [])
  
  return {
    openNote
  }
}

/**
 * 统计数据 Hook
 */
export function useStatistics() {
  const [statistics, setStatistics] = useState<Statistics | null>(null)
  const [loading, setLoading] = useState(false)
  
  const loadStatistics = useCallback(async () => {
    setLoading(true)
    try {
      const stats = await invoke<Statistics>('get_statistics')
      setStatistics(stats)
    } catch (error) {
      console.error('加载统计数据失败:', error)
    } finally {
      setLoading(false)
    }
  }, [])
  
  return {
    statistics,
    loading,
    loadStatistics
  }
}
```

```typescript
// src/stores/dailyStore.ts (Zustand 状态管理)

import { create } from 'zustand'
import { invoke } from '@tauri-apps/api/tauri'
import type { DailyLog } from '@/types'

interface DailyStore {
  logs: DailyLog[]
  currentLog: DailyLog | null
  loading: boolean
  error: string | null
  
  // Actions
  fetchLog: (date: string) => Promise<void>
  saveLog: (log: DailyLog) => Promise<void>
  setCurrentLog: (log: DailyLog | null) => void
}

export const useDailyStore = create<DailyStore>((set, get) => ({
  logs: [],
  currentLog: null,
  loading: false,
  error: null,
  
  fetchLog: async (date: string) => {
    set({ loading: true, error: null })
    try {
      const log = await invoke<DailyLog>('read_daily_log', { date })
      set({ currentLog: log, loading: false })
    } catch (error) {
      set({ error: error as string, loading: false })
    }
  },
  
  saveLog: async (log: DailyLog) => {
    set({ loading: true, error: null })
    try {
      await invoke('save_daily_log', { log })
      set({ currentLog: log, loading: false })
    } catch (error) {
      set({ error: error as string, loading: false })
    }
  },
  
  setCurrentLog: (log) => set({ currentLog: log })
}))
```

---

### 4. React 组件示例

```tsx
// src/pages/Dashboard.tsx

import React, { useEffect } from 'react'
import { Card, Row, Col, Statistic } from 'antd'
import { 
  ClockCircleOutlined, 
  FireOutlined, 
  BookOutlined, 
  CalendarOutlined 
} from '@ant-design/icons'
import { useStatistics, useFileWatcher } from '@/hooks/useTauriCommand'
import HeatmapCalendar from '@/components/charts/HeatmapCalendar'
import ProgressBar from '@/components/charts/ProgressBar'
import KnowledgeTree from '@/components/KnowledgeTree'

/**
 * 主仪表盘页面
 */
const Dashboard: React.FC = () => {
  const { statistics, loading, loadStatistics } = useStatistics()
  
  // 监听文件变化,自动重新加载
  useFileWatcher(() => {
    console.log('检测到文件变化,重新加载数据')
    loadStatistics()
  })
  
  useEffect(() => {
    loadStatistics()
  }, [loadStatistics])
  
  if (loading) {
    return <div className="loading">加载中...</div>
  }
  
  return (
    <div className="dashboard">
      <header className="dashboard-header">
        <h1>🎓 学习进度追踪</h1>
      </header>
      
      {/* 统计概览卡片 */}
      <Row gutter={[16, 16]} className="stats-overview">
        <Col span={6}>
          <Card>
            <Statistic
              title="总学习时长"
              value={statistics?.totalHours || 0}
              suffix="小时"
              prefix={<ClockCircleOutlined />}
            />
          </Card>
        </Col>
        <Col span={6}>
          <Card>
            <Statistic
              title="连续学习"
              value={statistics?.streak || 0}
              suffix="天"
              prefix={<FireOutlined />}
            />
          </Card>
        </Col>
        <Col span={6}>
          <Card>
            <Statistic
              title="知识点数"
              value={`${statistics?.completedPoints || 0}/${statistics?.totalPoints || 0}`}
              prefix={<BookOutlined />}
            />
          </Card>
        </Col>
        <Col span={6}>
          <Card>
            <Statistic
              title="本周时长"
              value={statistics?.weeklyHours || 0}
              suffix="小时"
              prefix={<CalendarOutlined />}
            />
          </Card>
        </Col>
      </Row>
      
      {/* 学习日历热力图 */}
      <Card 
        title="📅 学习日历" 
        className="section"
        style={{ marginTop: 24 }}
      >
        <HeatmapCalendar data={statistics?.heatmapData} />
      </Card>
      
      {/* 学习路径进度 */}
      <Card 
        title="🎯 学习路径进度" 
        className="section"
        style={{ marginTop: 24 }}
      >
        {statistics?.roadmaps?.map((roadmap) => (
          <ProgressBar
            key={roadmap.id}
            label={roadmap.name}
            progress={roadmap.progress}
            color={roadmap.color}
          />
        ))}
      </Card>
      
      {/* 知识树 */}
      <Card 
        title="🌳 知识树" 
        className="section"
        style={{ marginTop: 24 }}
      >
        <KnowledgeTree data={statistics?.knowledgeTree} />
      </Card>
    </div>
  )
}

export default Dashboard
```

```tsx
// src/components/charts/ProgressBar.tsx

import React from 'react'
import { Progress } from 'antd'

interface ProgressBarProps {
  label: string
  progress: number
  color?: string
}

/**
 * 进度条组件
 */
const ProgressBar: React.FC<ProgressBarProps> = ({ 
  label, 
  progress, 
  color = '#1890ff' 
}) => {
  return (
    <div className="progress-bar" style={{ marginBottom: 16 }}>
      <div className="progress-label" style={{ marginBottom: 8 }}>
        {label}
      </div>
      <Progress 
        percent={progress} 
        strokeColor={color}
        status="active"
      />
    </div>
  )
}

export default ProgressBar
```

```tsx
// src/App.tsx

import React from 'react'
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom'
import { Layout } from 'antd'
import Sidebar from '@/components/layout/Sidebar'
import Header from '@/components/layout/Header'
import Dashboard from '@/pages/Dashboard'
import DailyLog from '@/pages/DailyLog'
import RoadmapView from '@/pages/RoadmapView'
import KnowledgeTree from '@/pages/KnowledgeTree'
import NotesLibrary from '@/pages/NotesLibrary'
import Settings from '@/pages/Settings'
import './App.css'

const { Content } = Layout

/**
 * 应用根组件
 */
const App: React.FC = () => {
  return (
    <Router>
      <Layout style={{ minHeight: '100vh' }}>
        <Sidebar />
        <Layout>
          <Header />
          <Content style={{ margin: '24px 16px', padding: 24 }}>
            <Routes>
              <Route path="/" element={<Dashboard />} />
              <Route path="/daily" element={<DailyLog />} />
              <Route path="/roadmap" element={<RoadmapView />} />
              <Route path="/knowledge" element={<KnowledgeTree />} />
              <Route path="/notes" element={<NotesLibrary />} />
              <Route path="/settings" element={<Settings />} />
            </Routes>
          </Content>
        </Layout>
      </Layout>
    </Router>
  )
}

export default App
```

---

## 📦 实现计划

### 阶段一: 项目初始化 (1天)

**任务清单**:
- [ ] 使用 Tauri CLI 创建项目
- [ ] 配置 React + TypeScript + Vite
- [ ] 安装依赖 (Ant Design, ECharts, Zustand, etc.)
- [ ] 设置 Rust 后端依赖
- [ ] 配置 Tauri 权限 (文件系统访问)
- [ ] 创建基础目录结构

**命令**:
```bash
# 安装 Tauri CLI
npm install -g @tauri-apps/cli

# 创建 Tauri + React 项目
npm create tauri-app@latest visual-learn

# 选择:
# - Package manager: npm
# - UI template: React
# - TypeScript: Yes
# - Vite: Yes

cd visual-learn
npm install

# 安装前端依赖
npm install antd
npm install echarts echarts-for-react
npm install zustand
npm install react-router-dom
npm install @ant-design/icons
npm install react-markdown remark-gfm rehype-highlight
npm install cal-heatmap

# 开发模式运行
npm run tauri dev
```

**产出**:
- 可运行的 Tauri 应用骨架
- 基础页面布局

---

### 阶段二: 数据层开发 (2-3天)

**任务清单**:

#### Rust 后端
- [ ] 定义数据模型 (DailyLog, Roadmap, Statistics)
- [ ] 实现文件扫描功能 (`scan_study_notes`)
- [ ] 实现 Markdown 解析 (frontmatter + 正文)
- [ ] 实现每日记录 CRUD (`read_daily_log`, `save_daily_log`)
- [ ] 实现统计分析逻辑 (`get_statistics`)
- [ ] 实现文件监听 (`file_watcher`)
- [ ] 添加错误处理和日志

#### 数据目录
- [ ] 创建 `data/` 目录结构
- [ ] 编写示例每日记录 (5-10天)
- [ ] 创建 2-3 条学习路线图
- [ ] 生成知识库索引 JSON

**产出**:
- 完整的 Tauri Commands API
- 示例数据文件
- 能够读取和解析本地数据

---

### 阶段三: 前端开发 (3-5天)

**任务清单**:

#### 基础架构
- [ ] 配置 React Router (路由)
- [ ] 配置 Zustand (状态管理)
- [ ] 封装 Tauri Commands Hooks (`useTauriCommand.ts`)
- [ ] 定义 TypeScript 类型

#### 核心页面
- [ ] **Dashboard (主仪表盘)**
  - 学习概览卡片 (Ant Design Card + Statistic)
  - 学习日历热力图 (cal-heatmap)
  - 学习路径进度条
  - 学习趋势图 (ECharts)
  
- [ ] **DailyLog (每日记录)**
  - 日历选择器 (Ant Design DatePicker)
  - 记录列表
  - 记录详情编辑器
  - Markdown 预览 (react-markdown)
  
- [ ] **RoadmapView (学习路线)**
  - 路线列表
  - 进度可视化
  - 阶段任务列表
  
- [ ] **KnowledgeTree (知识树)**
  - 树状图展示 (Ant Design Tree)
  - 展开/折叠
  - 知识点详情
  - 点击打开笔记
  
- [ ] **NotesLibrary (笔记库)**
  - 自动扫描的笔记列表
  - 分类筛选
  - 搜索功能 (Ant Design Input.Search)
  - 点击打开笔记

#### 组件开发
- [ ] HeatmapCalendar (热力图)
- [ ] ProgressBar (进度条)
- [ ] TrendChart (趋势图)
- [ ] MarkdownPreview (Markdown 预览)
- [ ] QuickLogModal (快速记录弹窗)

**产出**:
- 完整的可视化界面
- 交互流畅的用户体验

---

### 阶段四: 桌面应用特性 (2-3天)

**任务清单**:

#### 系统集成
- [ ] **系统托盘**
  - 托盘图标
  - 右键菜单 (打开、快速记录、退出)
  - 显示今日学习时长
  
- [ ] **全局快捷键**
  - `Cmd+Shift+L` 打开快速记录窗口
  - 配置快捷键设置
  
- [ ] **桌面通知**
  - 学习提醒 ("该学习了!")
  - 目标达成通知 ("今日学习目标已完成!")
  
- [ ] **窗口管理**
  - 记住窗口位置和大小
  - 最小化到托盘

#### 文件操作
- [ ] 实时监听文件变化
- [ ] 点击笔记链接打开系统编辑器
- [ ] Markdown 文件预览

#### 数据导出
- [ ] 导出学习报告 (Markdown)
- [ ] 导出统计数据 (JSON)

**产出**:
- 完整的桌面应用功能
- 原生应用体验

---

### 阶段五: 优化与打包 (1-2天)

**任务清单**:

#### 性能优化
- [ ] 大文件扫描优化 (分批加载)
- [ ] 图表渲染优化 (虚拟滚动)
- [ ] 缓存机制 (避免重复计算)

#### 应用打包
- [ ] 配置应用图标
- [ ] 配置应用签名 (macOS)
- [ ] 打包 macOS 应用 (`.dmg`)
- [ ] 打包 Windows 应用 (`.msi`) (可选)

#### 文档
- [ ] 编写 README
- [ ] 编写使用文档
- [ ] 录制演示视频

**命令**:
```bash
# 打包应用
npm run tauri build

# 输出路径: src-tauri/target/release/bundle/
```

**产出**:
- 可安装的桌面应用
- 完整的使用文档

---

---

## 🚀 快速开始

### 开发环境准备

```bash
# 1. 安装 Rust (Tauri 依赖)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 2. 安装 Node.js (推荐使用 nvm)
# macOS:
brew install nvm
nvm install 20
nvm use 20

# 3. 安装 Tauri CLI
npm install -g @tauri-apps/cli

# 4. 验证安装
tauri --version
rustc --version
node --version
```

### 创建项目

```bash
# 进入项目目录
cd /Users/imber/Desktop/ai

# 创建 Tauri 应用
npm create tauri-app@latest visual-learn
# 选择: React + TypeScript

cd visual-learn

# 安装依赖
npm install

# 安装前端库
npm install antd @ant-design/icons
npm install echarts echarts-for-react
npm install zustand
npm install react-router-dom
npm install react-markdown remark-gfm rehype-highlight
npm install cal-heatmap

# 开发模式运行
npm run tauri dev
```

### 首次运行

1. 应用窗口会打开,显示默认页面
2. 修改 `src/App.tsx` 实时热更新
3. 修改 `src-tauri/src/main.rs` 需要重启应用

### 前端入口配置

```tsx
// src/main.tsx

import React from 'react'
import ReactDOM from 'react-dom/client'
import { ConfigProvider } from 'antd'
import zhCN from 'antd/locale/zh_CN'
import App from './App'
import './index.css'
import 'antd/dist/reset.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ConfigProvider locale={zhCN}>
      <App />
    </ConfigProvider>
  </React.StrictMode>,
)
```

### 项目配置

编辑 `src-tauri/tauri.conf.json`:

```json
{
  "build": {
    "beforeBuildCommand": "npm run build",
    "beforeDevCommand": "npm run dev",
    "devPath": "http://localhost:5173",
    "distDir": "../dist"
  },
  "package": {
    "productName": "Imber学习追踪",
    "version": "1.0.0"
  },
  "tauri": {
    "allowlist": {
      "all": false,
      "fs": {
        "all": true,
        "readFile": true,
        "writeFile": true,
        "readDir": true,
        "scope": [
          "$HOME/Desktop/imber/**",
          "$APPDATA/visual-learn/**"
        ]
      },
      "shell": {
        "open": true
      },
      "notification": {
        "all": true
      }
    },
    "systemTray": {
      "iconPath": "icons/icon.png",
      "iconAsTemplate": true
    },
    "windows": [
      {
        "title": "Imber学习追踪",
        "width": 1200,
        "height": 800,
        "resizable": true,
        "fullscreen": false
      }
    ]
  }
}
```

---

## 💡 扩展功能(后期)

### 桌面应用增强
1. **智能提醒系统**
   - 每日固定时间学习提醒
   - 长时间未学习提醒
   - 学习目标进度提醒
   
2. **数据同步**
   - iCloud 同步 (macOS)
   - Git 自动提交备份
   
3. **快捷操作**
   - 拖拽文件添加到学习记录
   - 截图自动添加到笔记
   - 剪贴板监听自动记录

4. **高级可视化**
   - 3D 知识图谱
   - 时间轴动画
   - 学习轨迹回放

### 智能功能
1. **AI 增强**
   - 自动总结每日学习内容
   - 智能推荐下一步学习内容
   - 生成周报/月报
   - 笔记智能关联

2. **知识复习**
   - 间隔重复算法 (Anki 风格)
   - 自动生成复习任务
   - 遗忘曲线追踪

3. **学习分析**
   - 学习效率分析
   - 最佳学习时间段识别
   - 知识点关联度分析

### 社交功能
1. **学习分享**
   - 生成学习成就图片
   - 导出到社交媒体
   
2. **多人协同** (可选)
   - 学习小组功能
   - 进度对比
   - 学习打卡

---

## 📋 配置文件示例

### `src-tauri/tauri.conf.json` (核心配置)

```json
{
  "build": {
    "beforeBuildCommand": "npm run build",
    "beforeDevCommand": "npm run dev",
    "devPath": "http://localhost:5173",
    "distDir": "../dist"
  },
  "package": {
    "productName": "Imber学习追踪",
    "version": "1.0.0"
  },
  "tauri": {
    "allowlist": {
      "fs": {
        "all": true,
        "scope": [
          "$HOME/Desktop/imber/**",
          "$APPDATA/visual-learn/**"
        ]
      },
      "shell": {
        "open": true
      },
      "notification": {
        "all": true
      },
      "globalShortcut": {
        "all": true
      }
    },
    "systemTray": {
      "iconPath": "icons/icon.png",
      "iconAsTemplate": true,
      "menuOnLeftClick": false
    },
    "windows": [
      {
        "title": "Imber学习追踪",
        "width": 1200,
        "height": 800,
        "minWidth": 800,
        "minHeight": 600,
        "resizable": true,
        "fullscreen": false,
        "center": true
      }
    ]
  }
}
```

### `config.yaml` (应用配置)

```yaml
# 学习进度追踪系统配置

# 基础配置
app:
  name: "Imber学习进度追踪"
  version: "1.0.0"
  theme: "light"  # light / dark / auto

# 路径配置
paths:
  studyRoot: "/Users/imber/Desktop/imber"
  dataRoot: "./data"

# 学习路线配置
roadmaps:
  - id: "backend-python"
    name: "Python后端开发"
    category: "后端"
    color: "#3776ab"
    targetDate: "2026-09-01"
  
  - id: "ai-programming"
    name: "AI编程"
    category: "AI"
    color: "#ff6b6b"
    targetDate: "2026-08-01"
  
  - id: "agent"
    name: "Agent开发"
    category: "AI"
    color: "#4ecdc4"
    targetDate: "2026-10-01"

# 学习目标
goals:
  dailyMinutes: 180       # 每日学习目标(分钟)
  weeklyHours: 21         # 每周学习目标(小时)
  monthlyHours: 90        # 每月学习目标(小时)

# 热力图配置
heatmap:
  levels:
    - { min: 0, max: 0, color: "#ebedf0", label: "无" }
    - { min: 1, max: 60, color: "#c6e48b", label: "1h以下" }
    - { min: 61, max: 120, color: "#7bc96f", label: "1-2h" }
    - { min: 121, max: 240, color: "#239a3b", label: "2-4h" }
    - { min: 241, max: 999, color: "#196127", label: "4h以上" }

# 提醒配置
notifications:
  enabled: true
  dailyReminder:
    enabled: true
    time: "20:00"         # 每天20:00提醒学习
    message: "该学习了! 今天还没记录学习内容哦"
  goalAchieved:
    enabled: true
    message: "🎉 恭喜! 今日学习目标已完成!"

# 快捷键配置
shortcuts:
  quickLog: "CmdOrCtrl+Shift+L"    # 快速记录
  openApp: "CmdOrCtrl+Shift+I"     # 打开主窗口

# 自动化配置
automation:
  autoScan: true                    # 自动扫描笔记目录
  scanInterval: 300                 # 扫描间隔(秒)
  autoBackup: true                  # 自动备份
  backupPath: "./backups"
  backupInterval: 86400             # 备份间隔(秒, 1天)
```

---

## ✅ 预期效果

完成后你将拥有一个功能完整的桌面应用:

### 核心体验
1. **原生应用体验**
   - 双击启动,像普通 Mac 应用一样使用
   - 启动快 (~1秒),内存占用小 (~50MB)
   - 可最小化到系统托盘,常驻后台

2. **自动化数据追踪**
   - 自动扫描 `/Users/imber/Desktop/imber` 目录
   - 实时监听文件变化,无需手动刷新
   - 自动解析 Markdown 笔记,生成统计数据

3. **直观的可视化**
   - GitHub 风格学习热力图,成就感满满
   - 各学习路径进度一目了然
   - 知识树清晰展示知识体系

4. **便捷的操作**
   - 点击笔记链接直接打开本地文件
   - 快捷键 `Cmd+Shift+L` 快速记录今日学习
   - 托盘显示今日学习时长

5. **智能提醒**
   - 每日学习提醒,培养习惯
   - 目标达成通知,及时反馈

### 技术优势
- ✅ 包体积小 (~10MB vs Electron ~120MB)
- ✅ 启动快,响应快
- ✅ 内存占用低
- ✅ 跨平台 (macOS/Windows/Linux)
- ✅ 数据本地存储,安全可控

### 数据洞察
- 📊 清晰了解自己的学习习惯
- 📈 追踪学习进度,及时调整策略
- 🎯 量化学习成果,建立正反馈
- 🔥 连续学习天数激励坚持

---

## 🎯 下一步行动

**方案 A: 快速原型 (推荐先做这个)**
1. ✅ 确认方案可行性
2. 🔨 搭建 Tauri + Vue3 基础框架
3. 📝 实现文件扫描功能
4. 📊 实现基础统计面板 (热力图 + 进度条)
5. ⚡ 能跑起来,验证可行性 (1-2天)

**方案 B: 完整开发**
在方案 A 基础上逐步完善:
1. 完整的可视化面板
2. 文件监听和实时更新
3. 笔记预览和编辑
4. 系统集成 (托盘、通知、快捷键)
5. 打包发布 (3-5天)

---

## 🤔 需要确认的问题

1. **是否需要 SQLite 数据库?**
   - 轻量方案: JSON 文件存储 (推荐先用这个)
   - 进阶方案: SQLite 数据库 (数据量大时使用)

2. **是否需要 AI 功能?**
   - 自动总结学习内容
   - 智能推荐下一步学习内容
   - (需要接入大模型 API,如 OpenAI/Claude)

3. **是否需要跨平台?**
   - 仅 macOS (更快,专注优化)
   - macOS + Windows (工作量 +30%)

4. **UI 风格偏好?**
   - 简洁现代风格 (推荐)
   - GitHub 风格
   - 自定义

---

## 🚀 准备好开始了吗?

我可以帮你:

### 立即开始
1. **搭建 Tauri 项目框架**
   - 创建项目
   - 配置开发环境
   - 安装依赖

2. **创建示例数据**
   - 生成 10 天学习记录样例
   - 创建 2-3 条学习路线

3. **实现第一个功能**
   - 文件扫描
   - 基础统计展示

### 或者先做准备
- 安装 Rust 和 Node.js
- 熟悉 Tauri 文档
- 准备一些真实的学习数据

告诉我你想从哪里开始! 🎉
