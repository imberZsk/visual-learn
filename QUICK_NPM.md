# ⚡ 5 秒快速开始

想直接用 `npm run tauri:dev`？只需一步：

## 安装 Rust（一次性操作，5-10分钟）

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
```

## 然后就可以用 npm 命令了！

```bash
npm run tauri:dev    # 启动应用
npm run dev          # 只看前端
npm run tauri:build  # 打包应用
```

---

## 为什么需要 Rust？

Tauri = React 前端 + Rust 后端

就像 Electron 需要 Node.js，Tauri 需要 Rust。

**优势**:
- ✅ 只安装一次
- ✅ 比 Electron 轻量 10 倍
- ✅ 之后直接用 npm 命令

---

## 详细说明

查看 `NPM_COMMANDS.md`

---

**安装完 Rust，`npm run tauri:dev` 就能用了！** 🚀
